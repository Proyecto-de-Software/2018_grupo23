<form method="post" action="{{ path('motivo_consulta_delete', {'id': motivo_consultum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motivo_consultum.id) }}">
    <button class="btn">Delete</button>
</form>
